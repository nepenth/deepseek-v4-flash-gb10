# 2026-08-14 stability + correctness campaign

Canonical operator ledger for the live rc7 Arm B + 1M serve. KB mirror:
https://kb.whyland.com/go/ds4-rc7-campaign

This repository is the source of truth. The KB page is a monitoring surface.

## Live winner (20:05Z restore)

- Image: `dspark-vllm-gb10:v0.27.1-gb10-rc7`
- Profile: `deepseek-v4-flash-0731-v0271-canary`
- `max_model_len=1048576`, seqs=6, batch=8192, GMU=0.84, `fp8_ds_mla`, DSpark k=5
- `#31` CPU thinking-budget hook **OFF** (`DSPARK_SKIP_ISSUE31_HOTFIX=1`)
- `DEFAULT_THINKING=off`
- suppress-stops 0.27.1 rewrite **ON** (`patches/hotfix-dsv4-suppress-stops-v0271.py`)
- V2 runner ON. Do not set `VLLM_USE_V2_MODEL_RUNNER=0`.

## What landed

| ID | Result |
|---|---|
| Arm A hook ON 32k×6 | **24.5 tok/s** median — 1.8× tax vs prior 44.6 |
| Arm B hook OFF 32k×6 @500k | **43.1 tok/s** (41.1–44.5), MTP 95.3% |
| 1M raise | KV ~2.74M / 2.61×. Exact-ceiling builder 400s (`1048578 > 1048576`) |
| 1.04M retrieval | PASS, 20.7 min, 3/3 needles |
| Phase 5 + encoding | 11/11 and 4/4 |
| Context ladder | 4k / 128k / 393k / 1.04M all needle-PASS |
| 3× restart greedy | r0–r3 identical |
| Phase 7 1k soak | 1000/1000 in 855.5 s |
| Cancel | two stream aborts then `42` twice |
| E7 C1 decode | 512=72.8, 8k=75.7, 32k=73.3, 128k=51.0 |
| DSpark lifetime | 54.9% accept; p0 93.8% → p4 35.1% |
| E10 exact 32k×6 @ fresh 1M | **46.36 / 46.38 / 48.48**, spread 1.05×, ITL 20.6–21.6 ms, MTP 95.1% |
| E11 exact 32k×6 repeat | **42.59 / 43.89 / 45.31**, spread 1.06×, ITL 22.1–23.5 ms, MTP 93.4% |

`CORRECTNESS_SAFE` (greedy + encoding + restart) and soak-stable on this envelope.

## Negative findings

- `#31` host-scan hook is a real 1.8× decode tax at seqs=6. Short C1 hid it.
- `vllm-switch` same-profile does **not** apply env changes. Force `docker rm -f` both ranks (or stop with `ENV_FILE` set). Stop script without `ENV_FILE` dies on `WORKER_HOST`.
- Afternoon `decode-bench.py` 32k×6 printed 33–37 tok/s. **E9/E10:** that was harness, not `max_model_len` and not a live regression. Exact `issue27` on fresh 1M is **46.4**.
- Five inference-time Triton kernels still JIT after official warmup (rejection/resample/gumbel/topk/local_logits). Patch 0029 not baked.
- V2 does not support `thinking_token_budget`. Do not invent a closer. Port Mia PR #48 only if `thinking=max` is required.
- Process: detached jobs without a collector stalled the day. Hindsight stays paused until Chris says restore.

## Hard don'ts

`VLLM_USE_V2_MODEL_RUNNER=0` · raise GMU · rename KV to `nvfp4_ds_mla` · k=7 · Anemll 0.25 rebase · invent thinking closer · restore Hindsight without Chris · upstream PR without unique local proof

## Next

1. 0029 is now **in-tree** (`runtime/patches/vllm/0029-…`) as a candidate. Do **not** bake until A/B vs this winner.
2. Port Mia #48 only if thinking=max is needed.
3. Hindsight coverage audit after restore.
4. Keep this file + `project-status.json` + README aligned after every loop.
