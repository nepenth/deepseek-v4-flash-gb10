# Ops note: 400k + explicit KV arena (2026-08-13)

Live rc7 (`dspark-vllm-gb10:v0.27.1-gb10-rc7`) was retuned without a rebuild.

1. `MAX_MODEL_LEN` 1,048,576 → **400000** (C3 geometry, not a 1M soak target).
2. `MAX_NUM_SEQS` stayed **6**.
3. Served names restored to `deepseek-ai/DeepSeek-V4-Flash-0731` and
   `deepseek-v4-flash-0731` so existing clients keep working.
4. First 400k boot without `--kv-cache-memory`: 18.18 GiB / **944,766** tokens
   (2.36×). That is C2, not C3. Token count is `concurrency × max_model_len`;
   do not reuse the old 1M-boot 1.85M figure.
5. Then `--kv-cache-memory=28235618304` (26.3 GiB, profiler "fully utilize"
   on that same boot). GMU stayed 0.84. Result: **1,366,743 tokens · 3.42×**.
   C3 full-window at 400k; not C4. Do not raise max length unless the pool
   is re-measured.

A/B harness telemetry landed in `bf9d524`. Spec-helper warmup patch 0029 is
draft-only and is **not** in this image.
